# VSCode_Extension_practice


** To geranae a new extension, you can use the following command: **

```npm install -g yo generator-code``` for to create new extension project.
```yo code``` to select extension type